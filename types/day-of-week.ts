/** Day of week master — Monday–Sunday. Global reference. */
export interface DayOfWeek {
  dayOfWeekId: number;
  dayOfWeekCode: string;
  dayOfWeekName: string;
  shortName: string;
  displayOrder: number;
  isActive: boolean;
}
