/** Tax calculation method lookup — Percentage / Fixed. Global. */
export interface TaxCalculationTypeLookup {
  taxCalculationTypeId: number;
  taxCalculationTypeCode: string;
  taxCalculationTypeName: string;
  isActive: boolean;
  displayOrder: number;
}
