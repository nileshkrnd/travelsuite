/** Allow/Block rule-direction lookup used by ServiceProductMarketRule. Global. */
export interface RuleType {
  ruleTypeId: number;
  ruleTypeCode: string;
  ruleTypeName: string;
  isActive: boolean;
}
